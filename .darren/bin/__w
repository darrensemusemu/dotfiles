#!/usr/bin/env bash
#
# create a new workspace

dir=$1

if [[ -z "$1" ]]; then
	dir=$(find . -type d | fzf -e)
	exit_code=$?
	if [[ $exit_code -ne 0 ]]; then
		echo "][ o7 ][ exiting ..."
		exit 1
	fi
elif [[ $1 ==  "." ]]; then
	dir=$PWD
fi

cd $dir
tmux new -s $(basename $dir)

